import { View, StyleSheet } from "react-native";

function OfferCard() {
  return <View style={styles.container}></View>;
}

export default OfferCard;

const styles = StyleSheet.create({
  container: {
    backgroundColor: "#FF2F7A",
    width: "100%",
    height: "26%",
    borderRadius: 20,
  },
});
